import java.util.Scanner;

public class findSmlDigToStr {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String str = sc.nextLine();
        int k = 3;//sc.nextInt();
        int min = 0;
        StringBuilder sb = new StringBuilder();
       for(int i=0;i<str.length()-3;i++){
        for(int j=i+2;j<str.length();j++){
            sb.append(str)
            String ss = zz
            int ans = Integer.parseInt(j, str.length());
            if(ans > min){
                min = ans;
            }
        }
        // int first = Integer.parseInt(str.substring(k, str.length()));
        System.out.println();
       }
    }
}
